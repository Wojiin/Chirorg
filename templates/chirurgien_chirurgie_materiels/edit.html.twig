{% extends 'base.html.twig' %}

{% block title %}Modifier une association{% endblock %}

{% block body %}
<h1>Modifier l'association</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('app_chirurgien_chirurgie_materiels') }}" class="btn btn-secondary mt-3">Retour à la liste</a>
{% endblock %}
